import React from 'react';
import OriginalCodeBlock from '@theme-original/CodeBlock';
import '../prism-caddyfile';

export default function CodeBlock(props) {
  return <OriginalCodeBlock {...props} />;
}
